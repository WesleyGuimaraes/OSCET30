// Vercel Web Analytics initialization
// This module imports and initializes Vercel Analytics for the static site
import { inject } from '@vercel/analytics';

// Initialize analytics tracking
inject();

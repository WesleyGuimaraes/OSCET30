# SO / editores
.DS_Store
Thumbs.db
desktop.ini

# logs e temporários
*.log
*.tmp

# dependências (não usadas neste projeto estático, mas por garantia)
node_modules/

# saída combinada do build (gerada pela Vercel, não versionar)
public_combined/

# configurações locais do Claude Code
.claude/
